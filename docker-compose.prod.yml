services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    volumes: []

  frontend:
    build:
      target: prod
    ports:
      - "4173:80"
    volumes: []
    depends_on:
      - backend
